// Code generated by goa v2.0.0-wip, DO NOT EDIT.
//
// calcserver HTTP client CLI support package
//
// Command:
// $ goa gen goa.design-micro/backend/services/calcsvc/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	calcsvcc "goa.design-micro/backend/services/calcsvc/gen/http/calcsvc/client"
	goa "goa.design/goa"
	goahttp "goa.design/goa/http"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `calcsvc (add|multiply)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` calcsvc add --a 3793862871819669726 --b 8399553735696626949` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		calcsvcFlags = flag.NewFlagSet("calcsvc", flag.ContinueOnError)

		calcsvcAddFlags = flag.NewFlagSet("add", flag.ExitOnError)
		calcsvcAddAFlag = calcsvcAddFlags.String("a", "REQUIRED", "Left operand")
		calcsvcAddBFlag = calcsvcAddFlags.String("b", "REQUIRED", "Right operand")

		calcsvcMultiplyFlags    = flag.NewFlagSet("multiply", flag.ExitOnError)
		calcsvcMultiplyBodyFlag = calcsvcMultiplyFlags.String("body", "REQUIRED", "")
	)
	calcsvcFlags.Usage = calcsvcUsage
	calcsvcAddFlags.Usage = calcsvcAddUsage
	calcsvcMultiplyFlags.Usage = calcsvcMultiplyUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "calcsvc":
			svcf = calcsvcFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "calcsvc":
			switch epn {
			case "add":
				epf = calcsvcAddFlags

			case "multiply":
				epf = calcsvcMultiplyFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "calcsvc":
			c := calcsvcc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "add":
				endpoint = c.Add()
				data, err = calcsvcc.BuildAddPayload(*calcsvcAddAFlag, *calcsvcAddBFlag)
			case "multiply":
				endpoint = c.Multiply()
				data, err = calcsvcc.BuildMultiplyPayload(*calcsvcMultiplyBodyFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// calcsvcUsage displays the usage of the calcsvc command and its subcommands.
func calcsvcUsage() {
	fmt.Fprintf(os.Stderr, `The Calculator Service performs operations on numbers
Usage:
    %s [globalflags] calcsvc COMMAND [flags]

COMMAND:
    add: Add implements add.
    multiply: Multiply implements multiply.

Additional help:
    %s calcsvc COMMAND --help
`, os.Args[0], os.Args[0])
}
func calcsvcAddUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] calcsvc add -a INT -b INT

Add implements add.
    -a INT: Left operand
    -b INT: Right operand

Example:
    `+os.Args[0]+` calcsvc add --a 3793862871819669726 --b 8399553735696626949
`, os.Args[0])
}

func calcsvcMultiplyUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] calcsvc multiply -body JSON

Multiply implements multiply.
    -body JSON: 

Example:
    `+os.Args[0]+` calcsvc multiply --body '{
      "a": 3219793201326175278,
      "b": 8803302123552712831
   }'
`, os.Args[0])
}
